<div class="pageMessage full bluegray center">
  Tap a program to explore degrees & salaries
</div>
<div class="full padded">
  <div class="clusters">
    <ul>{{#each cluster in arrangedContent}}
      <li>
        {{#if cluster.jobGrowth}}<i {{action "explainJobGrowth"}} class="jobGrowth material-icons">&#xE8E5;</i>{{/if}}
        {{#if cluster.salaryGrowth}}<i {{action "explainSalaryGrowth"}} class="salaryGrowth material-icons">&#xE227;</i>{{/if}}
        <span {{action "forwardToClusterDetail" cluster.id}}>{{cluster.name}}</span>
      </li>
    {{/each}}</ul>
  </div>
</div>